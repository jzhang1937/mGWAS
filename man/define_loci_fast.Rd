% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finemap.R
\name{define_loci_fast}
\alias{define_loci_fast}
\title{Coordinate-free locus definition by greedy clumping + neighborhood expansion
(memory-light version: computes r^2 on the fly from genotypes, never
materializes a p x p matrix)}
\usage{
define_loci_fast(
  pvals,
  X,
  sig_thr = 5e-08,
  r2_thr = 0.1,
  already_scaled = FALSE
)
}
\arguments{
\item{pvals}{length-p numeric vector of p-values}

\item{X}{n x p genotype matrix (samples x variants), matching
column order of pvals. Must be complete (no NA).}

\item{sig_thr}{significance threshold for lead variants (default 5e-8)}

\item{r2_thr}{r^2 threshold for clumping and neighborhood (default 0.1)}

\item{already_scaled}{set TRUE if X is already column-standardized
(mean 0, sd 1) to skip re-scaling it internally}
}
\value{
list of loci, each with:
$lead     : index of lead variant
$variants : all variant indices in locus (lead + correlated neighbors)
}
\description{
Coordinate-free locus definition by greedy clumping + neighborhood expansion
(memory-light version: computes r^2 on the fly from genotypes, never
materializes a p x p matrix)
}
