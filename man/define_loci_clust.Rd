% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finemap.R
\name{define_loci_clust}
\alias{define_loci_clust}
\title{Define loci by hierarchical clustering on r² distance (no positional structure)}
\usage{
define_loci_clust(R2, r2_thr = 0.3, method = "average")
}
\arguments{
\item{R2}{p x p matrix of r² values between variants}

\item{r2_thr}{r² threshold — variants with r² > r2_thr end up in same cluster (default 0.3)}

\item{method}{linkage method: "average", "complete", or "single" (default "average")}
}
\value{
list of loci, each with:
$leads    : index of the variant with highest mean r² to cluster members (medoid)
$variants : all variant indices in locus
}
\description{
Define loci by hierarchical clustering on r² distance (no positional structure)
}
